import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    container: {
        position: "absolute", 
        bottom: 10, 
        right: 10, 
        zIndex: 999
    },
    icon: {
        width: 64,
        height: 64,
        tintColor: "#ffcc00"
    }
});